version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./core:/app/
    command: python manage.py runserver 0.0.0.0:8000
    # environment:
    #   - name=value
    container_name: backend
    environment:
      - SECRET_KEY=test
      - DEBUG=True
